// today learn about string
const h="hello"
const w="world"
// method to join two variable
const res=h+ " " +w
const res2=`${h} ${w}`
console.log(res);
console.log(res2);
// find the ength
let len=h.length
console.log(len);
//  find the lenght
// answer -1 aauxa asko matlab xaena ho
const ind=h.indexOf("p")
console.log(ind);
